#include <sys/socket.h>
#include <unistd.h>

#include <server/http.h>
#include <server/parser.h>
#include <server/server.h>
#include <server/executor.h>

char* home(http *server, request *req);
char* about(http *server, request *req);

char* home(http *server, request *req) {

}

char* info(http *server, request *req) {

}


int main(void){ 

}

