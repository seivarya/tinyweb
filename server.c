#include <stdio.h>
#include <sys/socket.h>
#include <netinet/in.h>


int main(void) {
	if(sizeof(struct sockaddr) > sizeof(struct sockaddr_in) {
		printf("sockaddr's size is > sockaddr_in's");
	}
}






